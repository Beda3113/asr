# @package _global_

instance_transforms:
  train:
    get_spectrogram:
      _target_: torchaudio.transforms.MelSpectrogram
      sample_rate: 16000
      n_mels: 80
      n_fft: 400
      hop_length: 160
      power: 1.0
  inference:
    get_spectrogram:
      _target_: torchaudio.transforms.MelSpectrogram
      sample_rate: 16000
      n_mels: 80
      n_fft: 400
      hop_length: 160
      power: 1.0

batch_transforms:
  train: null
  inference: null